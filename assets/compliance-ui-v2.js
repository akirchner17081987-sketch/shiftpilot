// SchichtFunk Compliance V2 – UI
(function(){
  const C=window.SFCompliance;if(!C)return;
  const terminal=new Set(['APPLIED','REJECTED','CANCELLED','SUPERSEDED']);
  const statusLabel={PENDING_EMPLOYEE:'Mitarbeiterbestätigung offen',PENDING_WORKS_COUNCIL:'Betriebsrat offen',READY_TO_APPLY:'Bereit zur Übernahme',BLOCKED:'Blockiert',APPLIED:'Übernommen',REJECTED:'Abgelehnt',CANCELLED:'Storniert',SUPERSEDED:'Überholt'};
  const actionLabel={CREATE:'Besetzung ergänzen',UPDATE:'Schicht ändern',DELETE:'Schicht entfernen'};
  const reasonOptions=['Krankheitsausfall','Kurzfristiger Personalausfall','Auftragsänderung','Kundenanforderung','Zusätzlicher Personalbedarf','Mitarbeiterwunsch','Schichttausch','Planungsfehler','Außergewöhnliches Ereignis','Sonstiges'];

  function injectStyles(){if(document.getElementById('sfComplianceCss'))return;const s=document.createElement('style');s.id='sfComplianceCss';s.textContent=`
.sf-compliance-backdrop{position:fixed;inset:0;z-index:12000;background:rgba(1,6,12,.62);backdrop-filter:blur(5px);display:flex;justify-content:flex-end}
.sf-compliance-drawer{width:min(620px,96vw);height:100%;overflow:auto;background:linear-gradient(180deg,#0c1a29,#08131f);border-left:1px solid #29445c;box-shadow:-24px 0 80px rgba(0,0,0,.5);display:flex;flex-direction:column}
.sf-c-head{display:flex;justify-content:space-between;gap:16px;padding:22px 24px;border-bottom:1px solid #20364b;position:sticky;top:0;background:#0b1826;z-index:2}.sf-c-head h2{margin:3px 0 4px;font-size:22px}.sf-c-head p{margin:0;color:#8fa5bd;font-size:12px}.sf-c-close{width:38px;height:38px;border:1px solid #294159;background:#101f2f;color:#a9bdd0;border-radius:9px}
.sf-c-body{padding:20px 24px;display:flex;flex-direction:column;gap:14px}.sf-c-card{border:1px solid #263e55;background:#0d1b2a;border-radius:12px;padding:14px}.sf-c-card h3{font-size:13px;margin:0 0 10px}.sf-c-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.sf-c-old,.sf-c-new{padding:12px;border-radius:10px;border:1px solid #294159;background:#0a1623}.sf-c-new{border-color:#216b5d;background:#0d2a25}.sf-c-old small,.sf-c-new small{display:block;color:#88a0b8;margin-bottom:5px}.sf-c-old b,.sf-c-new b{display:block}.sf-result{border-radius:11px;padding:13px 14px;font-weight:800}.sf-result.GREEN{border:1px solid #216b59;background:#0e2d26;color:#7aefd0}.sf-result.REVIEW{border:1px solid #76562a;background:#302516;color:#ffd18c}.sf-result.BLOCK{border:1px solid #7a2d3c;background:#321822;color:#ff98a6}
.sf-findings{display:flex;flex-direction:column;gap:7px}.sf-finding{display:grid;grid-template-columns:28px 1fr;gap:9px;padding:9px 10px;border:1px solid #22384e;border-radius:9px;background:#0a1724;font-size:12px}.sf-finding.PASS i{color:#4ce0b9}.sf-finding.REVIEW i{color:#ffbf5d}.sf-finding.BLOCK i{color:#ff7183}.sf-finding i{font-style:normal;font-weight:900}.sf-finding small{display:block;color:#7891a9;margin-top:3px}
.sf-c-field{display:flex;flex-direction:column;gap:6px}.sf-c-field label{font-size:11px;color:#9eb2c8;font-weight:800}.sf-c-field input,.sf-c-field select,.sf-c-field textarea{width:100%;background:#091624;border:1px solid #294159;color:#eef7ff;border-radius:9px;padding:10px 11px;outline:none}.sf-c-field input:focus,.sf-c-field select:focus,.sf-c-field textarea:focus{border-color:#2ed9b8;box-shadow:0 0 0 3px rgba(46,217,184,.1)}
.sf-c-foot{display:flex;justify-content:flex-end;gap:9px;padding:16px 24px;border-top:1px solid #20364b;background:#091522;position:sticky;bottom:0;margin-top:auto}.sf-c-note{font-size:11px;color:#8298af;line-height:1.5}.sf-c-note strong{color:#bdd0e2}.sf-compliance-btn{position:relative}.sf-open-count{display:inline-flex;min-width:19px;height:19px;align-items:center;justify-content:center;border-radius:99px;background:#3b2917;color:#ffc56e;font-size:10px;font-weight:900;margin-left:5px}.sf-published{border-color:#236b5b!important;color:#68e5c4!important;background:#0d2c26!important}
.sf-pending-assignment{outline:2px solid #e5a845!important;outline-offset:1px}.sf-pending-badge{position:absolute;right:4px;top:4px;z-index:2;background:#3b2917;border:1px solid #7a5728;color:#ffd08a;border-radius:99px;padding:2px 5px;font-size:8px;font-weight:900}
.sf-center{width:min(1040px,96vw);max-height:92vh;overflow:auto;background:#0a1724;border:1px solid #29445c;border-radius:16px;box-shadow:0 28px 90px rgba(0,0,0,.6)}.sf-center-backdrop{position:fixed;inset:0;z-index:12010;background:rgba(1,6,12,.72);display:grid;place-items:center;padding:18px}.sf-center-head{display:flex;justify-content:space-between;padding:20px 22px;border-bottom:1px solid #20364b}.sf-center-head h2{margin:3px 0}.sf-center-body{padding:18px 22px}.sf-tabs-title{font-size:12px;color:#8fa5bd;font-weight:900;letter-spacing:.08em;margin:15px 0 8px}.sf-request{display:grid;grid-template-columns:1.3fr .8fr 1fr auto;gap:12px;align-items:center;border:1px solid #243b52;background:#0d1b2a;border-radius:11px;padding:12px;margin-bottom:8px}.sf-request b{display:block}.sf-request small{display:block;color:#849bb2;margin-top:3px}.sf-status{display:inline-flex;width:max-content;border-radius:99px;padding:5px 8px;font-size:10px;font-weight:900;background:#203149;color:#bfd0e1}.sf-status.pending{background:#3b2917;color:#ffd18c}.sf-status.ready{background:#10382f;color:#6de5c3}.sf-status.blocked,.sf-status.rejected{background:#3a1b25;color:#ff8b9b}.sf-req-actions{display:flex;gap:6px;flex-wrap:wrap;justify-content:flex-end}.sf-mini{min-height:32px!important;padding:6px 8px!important;font-size:11px!important}.sf-audit{font-size:11px;color:#91a8bf;border-top:1px solid #1d3348;padding:8px 0}.sf-audit b{color:#c9d9e8}.sf-settings-card{margin-top:14px}.sf-policy-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.sf-legal-note{padding:10px 12px;border:1px solid #5d4b2c;background:#2a2318;color:#e5c98f;border-radius:9px;font-size:11px;margin-top:10px}
@media(max-width:760px){.sf-c-grid,.sf-policy-grid{grid-template-columns:1fr}.sf-request{grid-template-columns:1fr}.sf-req-actions{justify-content:flex-start}.sf-c-body{padding:16px}.sf-c-head,.sf-c-foot{padding-left:16px;padding-right:16px}}
`;document.head.appendChild(s)}

  C.closeChangeDrawer=()=>document.getElementById('sfComplianceBackdrop')?.remove();
  window.spCloseChangeDrawer=C.closeChangeDrawer;
  function assignmentText(x){if(!x)return'–';const e=employees.find(y=>y.id===x.employeeId);return `${x.type||''} · ${e?e.first+' '+e.last:'Mitarbeiter'} · ${C.fmt(x.date)} · ${x.start||''}–${x.end||''}`}

  C.openChangeDrawer=payload=>{
    injectStyles();C.closeChangeDrawer();const result=C.complianceCheck(payload),old=payload.assignment?C.snapshot(payload.assignment):null;
    const proposed=payload.action==='DELETE'?null:{employeeId:payload.employeeId,type:payload.type,date:payload.date,start:payload.start,end:payload.end};
    const levelText=result.level==='BLOCK'?'Änderung blockiert':result.level==='REVIEW'?'Prüfung / Freigabe erforderlich':'Keine Standardverletzung erkannt';
    const el=document.createElement('div');el.id='sfComplianceBackdrop';el.className='sf-compliance-backdrop';
    el.innerHTML=`<div class="sf-compliance-drawer" role="dialog" aria-modal="true"><div class="sf-c-head"><div><div class="eyebrow">🛡 COMPLIANCE-PRÜFUNG</div><h2>${C.esc(actionLabel[payload.action]||'Schichtänderung')}</h2><p>Veröffentlichte Schichten werden nicht direkt überschrieben.</p></div><button class="sf-c-close" onclick="spCloseChangeDrawer()">✕</button></div><div class="sf-c-body">
      <div class="sf-c-grid"><div class="sf-c-old"><small>Bisher gültig</small><b>${C.esc(assignmentText(old))}</b></div><div class="sf-c-new"><small>${payload.action==='DELETE'?'Geplant':'Vorgeschlagen'}</small><b>${C.esc(payload.action==='DELETE'?'Schicht entfernen':assignmentText(proposed))}</b></div></div>
      <div class="sf-result ${result.level}">${result.level==='GREEN'?'✓':result.level==='REVIEW'?'⚠':'⛔'} ${C.esc(levelText)}</div>
      <div class="sf-c-card"><h3>Automatische Prüfungen</h3><div class="sf-findings">${result.findings.map(f=>`<div class="sf-finding ${f.status}"><i>${f.status==='PASS'?'✓':f.status==='REVIEW'?'⚠':'⛔'}</i><div>${C.esc(f.text)}${f.basis?`<small>${C.esc(f.basis)}</small>`:''}</div></div>`).join('')}</div></div>
      <div class="sf-c-card"><h3>Begründung & Dokumentation</h3><div class="sf-c-field"><label>Grund der Änderung *</label><select id="sfReason"><option value="">Bitte auswählen …</option>${reasonOptions.map(x=>`<option>${C.esc(x)}</option>`).join('')}</select></div><div class="sf-c-field" style="margin-top:10px"><label>War der Grund bei Veröffentlichung vorhersehbar?</label><select id="sfPredictable"><option value="NO">Nein</option><option value="PARTLY">Teilweise</option><option value="YES">Ja</option></select></div><div class="sf-c-field" style="margin-top:10px"><label>Interne Begründung / Hinweis</label><textarea id="sfReasonText" rows="3" placeholder="z. B. Krankmeldung kurzfristig eingegangen …"></textarea></div></div>
      <div class="sf-c-note"><strong>Hinweis:</strong> Die Anzeige ersetzt keine Einzelfall-Rechtsberatung. Unternehmensschwellen wie 24/48 Stunden sind betriebliche Risikoregeln, keine allgemeinen gesetzlichen Fristen. Gesetzliche/tarifliche Ausnahmen müssen separat hinterlegt und geprüft werden.</div>
    </div><div class="sf-c-foot"><button class="ghost" onclick="spCloseChangeDrawer()">Abbrechen</button>${result.level==='BLOCK'?'<button class="primary" disabled title="Blockierende Standardregel">Änderung nicht möglich</button>':`<button class="primary" id="sfSubmitChange">${result.employeeApproval?'Änderung anfragen':result.worksCouncil?'Freigabe anfordern':'Änderung speichern'}</button>`}</div></div>`;
    document.body.appendChild(el);
    el.querySelector('#sfSubmitChange')?.addEventListener('click',()=>{
      const reasonCode=el.querySelector('#sfReason').value,reasonText=el.querySelector('#sfReasonText').value.trim(),predictable=el.querySelector('#sfPredictable').value;
      if(C.policy.requireReason&&!reasonCode){alert('Bitte einen Grund für die veröffentlichte Schichtänderung auswählen.');return}
      const req=C.createRequest(payload,{reasonCode,reasonText,predictable},result);C.closeChangeDrawer();
      if(req.status==='READY_TO_APPLY')C.applyRequest(req.id);else{C.refresh();C.toast('Änderung angefragt',statusLabel[req.status]||req.status);C.openComplianceCenter()}
    });
  };

  function requestStatusClass(s){return s==='READY_TO_APPLY'?'ready':s==='BLOCKED'?'blocked':s==='REJECTED'?'rejected':s.startsWith('PENDING_')?'pending':''}
  function requestActions(r){
    if(r.status==='PENDING_EMPLOYEE')return `<button class="primary sf-mini" onclick="spApproveRequest('${r.id}','employee')">Bestätigung erfassen</button><button class="ghost sf-mini" onclick="spRejectRequest('${r.id}','employee')">Ablehnung</button>`;
    if(r.status==='PENDING_WORKS_COUNCIL')return `<button class="primary sf-mini" onclick="spApproveRequest('${r.id}','worksCouncil')">BR-Freigabe erfassen</button><button class="ghost sf-mini" onclick="spRejectRequest('${r.id}','worksCouncil')">Ablehnung</button>`;
    if(r.status==='READY_TO_APPLY')return `<button class="primary sf-mini" onclick="spApplyChangeRequest('${r.id}')">Jetzt übernehmen</button>`;
    if(!terminal.has(r.status)&&r.status!=='BLOCKED')return `<button class="ghost sf-mini" onclick="spCancelChangeRequest('${r.id}')">Stornieren</button>`;
    return '';
  }
  function requestHtml(r){const e=employees.find(x=>x.id===r.employeeId),p=r.proposed||r.old||{};return `<div class="sf-request"><div><b>${C.esc(actionLabel[r.action]||r.action)} · ${C.esc(e?e.first+' '+e.last:'Mitarbeiter')}</b><small>${C.esc(p.type||r.type||'')} · ${C.esc(C.fmt(p.date||r.date))}${p.start?' · '+C.esc(p.start)+'–'+C.esc(p.end):''}</small><small>Grund: ${C.esc(r.reasonCode||'–')}</small></div><div><span class="sf-status ${requestStatusClass(r.status)}">${C.esc(statusLabel[r.status]||r.status)}</span><small>${new Date(r.requestedAt).toLocaleString('de-DE')}</small></div><div><small>Compliance</small><b>${r.complianceStatus==='GREEN'?'🟢':r.complianceStatus==='REVIEW'?'🟠':'🔴'} ${C.esc(r.complianceStatus)}</b>${Number.isFinite(r.noticeHours)?`<small>Vorlauf: ${r.noticeHours.toFixed(1)} Std.</small>`:''}</div><div class="sf-req-actions">${requestActions(r)}</div></div>`}
  C.closeComplianceCenter=()=>document.getElementById('sfComplianceCenterBackdrop')?.remove();window.spCloseComplianceCenter=C.closeComplianceCenter;
  C.openComplianceCenter=()=>{
    injectStyles();C.closeComplianceCenter();const open=C.requests.filter(r=>!terminal.has(r.status)),history=C.requests.filter(r=>terminal.has(r.status)).slice(0,30);
    const b=document.createElement('div');b.id='sfComplianceCenterBackdrop';b.className='sf-center-backdrop';b.innerHTML=`<div class="sf-center"><div class="sf-center-head"><div><div class="eyebrow">🛡 COMPLIANCE</div><h2>Schichtänderungen & Audit</h2><p style="margin:0;color:#8fa5bd">Offene Freigaben, blockierte Änderungen und Verlauf.</p></div><button class="sf-c-close" onclick="spCloseComplianceCenter()">✕</button></div><div class="sf-center-body"><div class="sf-tabs-title">OFFENE VORGÄNGE · ${open.length}</div>${open.length?open.map(requestHtml).join(''):'<div class="notice">✓ Keine offenen Änderungsvorgänge.</div>'}<div class="sf-tabs-title">VERLAUF</div>${history.length?history.map(requestHtml).join(''):'<div class="empty">Noch kein Änderungsverlauf vorhanden.</div>'}<div class="sf-tabs-title">AUDIT-LOG</div>${C.auditEvents.slice(0,30).map(a=>`<div class="sf-audit"><b>${C.esc(a.event)}</b> · ${new Date(a.at).toLocaleString('de-DE')} · ${C.esc(a.actor||'System')}</div>`).join('')||'<div class="empty">Noch keine Audit-Ereignisse.</div>'}</div></div>`;document.body.appendChild(b)
  };window.spOpenComplianceCenter=C.openComplianceCenter;

  C.publishCurrentWeek=()=>{
    const key=C.weekKey(C.iso(weekStart));if(C.publications[key]?.publishedAt){C.toast('Dienstplan bereits veröffentlicht',new Date(C.publications[key].publishedAt).toLocaleString('de-DE'));return}
    if(!confirm('Dienstplan für die aktuelle Woche veröffentlichen?\n\nAb diesem Zeitpunkt laufen spätere Änderungen über die Compliance-Prüfung und werden protokolliert.'))return;
    const now=new Date().toISOString();C.publications[key]={publishedAt:now,publishedBy:'Administrator'};const dates=currentWeekDates().map(C.iso);assignments.filter(a=>dates.includes(a.date)).forEach(a=>{a.publishedAt=a.publishedAt||now;a.version=a.version||1});C.audit('PLAN_PUBLISHED',key,{dates});C.refresh();C.updateScheduleControls();C.toast('Dienstplan veröffentlicht','Spätere Änderungen werden ab jetzt als Änderungsvorgang dokumentiert.')
  };window.spPublishCurrentWeek=C.publishCurrentWeek;

  C.updateScheduleControls=()=>{
    injectStyles();const tb=document.querySelector('#view-schedule .cal-toolbar');if(!tb)return;let box=document.getElementById('sfComplianceToolbar');if(!box){box=document.createElement('span');box.id='sfComplianceToolbar';box.style.cssText='display:inline-flex;gap:7px;align-items:center;margin-left:7px';tb.appendChild(box)}
    const key=C.weekKey(C.iso(weekStart)),pub=C.publications[key],open=C.requests.filter(r=>!terminal.has(r.status)).length;
    box.innerHTML=`${pub?`<button class="ghost sf-published" title="Veröffentlicht am ${C.esc(new Date(pub.publishedAt).toLocaleString('de-DE'))}">✓ Veröffentlicht</button>`:`<button class="ghost" onclick="spPublishCurrentWeek()">◉ Dienstplan veröffentlichen</button>`}<button class="ghost sf-compliance-btn" onclick="spOpenComplianceCenter()">🛡 Compliance <span class="sf-open-count">${open}</span></button>`;
  };
  C.decorateAssignments=()=>{
    document.querySelectorAll('#calendarGrid .sf-pending-badge').forEach(x=>x.remove());document.querySelectorAll('#calendarGrid .sf-pending-assignment').forEach(x=>x.classList.remove('sf-pending-assignment'));
    C.requests.filter(r=>r.assignmentId&&!terminal.has(r.status)).forEach(r=>{const el=[...document.querySelectorAll('#calendarGrid .assignment')].find(x=>(x.getAttribute('onclick')||'').includes(`'${r.assignmentId}'`));if(el){el.classList.add('sf-pending-assignment');el.style.position='absolute';el.insertAdjacentHTML('beforeend','<span class="sf-pending-badge">Änderung offen</span>')}})
  };
  if(typeof window.renderCalendar==='function'){const base=window.renderCalendar;window.renderCalendar=function(){const r=base.apply(this,arguments);setTimeout(()=>{C.updateScheduleControls();C.decorateAssignments()},0);return r}}

  function installEmployeeWorkModel(){
    const form=document.getElementById('employeeForm');if(!form||document.getElementById('workTimeModel'))return;const full=form.querySelector('label.full');const lab=document.createElement('label');lab.innerHTML='Arbeitszeitmodell<select id="workTimeModel"><option value="SHIFT">Schichtarbeit</option><option value="STANDARD">Feste Arbeitszeit</option><option value="FLEXIBLE">Flexible Schichten</option><option value="ON_CALL">Arbeit auf Abruf (§ 12 TzBfG)</option></select>';form.insertBefore(lab,full||null);
    if(typeof window.selectEmployee==='function'){const base=window.selectEmployee;window.selectEmployee=function(id){const r=base.apply(this,arguments),e=employees.find(x=>x.id===id);document.getElementById('workTimeModel').value=e?.workTimeModel||'SHIFT';return r}}
    if(typeof window.clearEmployeeForm==='function'){const base=window.clearEmployeeForm;window.clearEmployeeForm=function(){const r=base.apply(this,arguments);document.getElementById('workTimeModel').value='SHIFT';return r}}
    if(typeof window.saveEmployee==='function'){const base=window.saveEmployee;const wrapped=function(){const model=document.getElementById('workTimeModel')?.value||'SHIFT';const r=base.apply(this,arguments);const e=employees.find(x=>x.id===selectedEmployeeId);if(e){e.workTimeModel=model;saveAll()}return r};window.saveEmployee=wrapped;document.getElementById('saveEmployeeBtn').onclick=wrapped}
    if(selectedEmployeeId){const e=employees.find(x=>x.id===selectedEmployeeId);document.getElementById('workTimeModel').value=e?.workTimeModel||'SHIFT'}
  }

  C.renderComplianceSettings=target=>{
    const view=document.getElementById('view-settings'),modern=document.getElementById('sfSettingsV2'),host=target||(!modern?view:null);if(!view||!host)return;let card=document.getElementById('sfComplianceSettings');if(!card){card=document.createElement('div');card.id='sfComplianceSettings';card.className='card form-card sf-settings-card'}host.appendChild(card);
    card.innerHTML=`<h3>🛡 Audit & Compliance</h3><p style="color:var(--muted)">Betriebliche Warnschwellen, Freigabeworkflow und protokollierte Änderungen.</p><div class="sf-policy-grid"><div class="sf-c-field"><label>Kurzfristig ab weniger als (Std.)</label><input id="sfShortHours" type="number" min="1" value="${Number(C.policy.shortNoticeHours)}"></div><div class="sf-c-field"><label>Sehr kurzfristig ab weniger als (Std.)</label><input id="sfCriticalHours" type="number" min="1" value="${Number(C.policy.criticalNoticeHours)}"></div><div class="sf-c-field"><label>Mitarbeiterbestätigung unter (Std.)</label><input id="sfApprovalHours" type="number" min="0" value="${Number(C.policy.employeeConfirmationUnderHours)}"></div></div><label class="check" style="display:inline-flex;margin-top:12px"><input type="checkbox" id="sfWorksCouncil" ${C.policy.worksCouncilEnabled?'checked':''}> Betriebsrat vorhanden / Freigabeworkflow aktiv</label><div class="sf-legal-note">24/48 Stunden sind hier <b>Unternehmensregeln</b>, keine allgemeine gesetzliche Änderungsfrist. Arbeit auf Abruf wird separat mit der Vier-Tage-Prüfung gekennzeichnet.</div><div class="form-actions"><button class="primary" onclick="switchView('audit')">Audit-Logs öffnen</button><button class="ghost" onclick="spOpenComplianceCenter()">Schichtänderungen öffnen</button><button class="ghost" onclick="spSaveComplianceSettings()">Prüfregeln speichern</button></div>`
  };
  window.spSaveComplianceSettings=()=>{C.policy.shortNoticeHours=Math.max(1,Number(document.getElementById('sfShortHours')?.value||48));C.policy.criticalNoticeHours=Math.max(1,Number(document.getElementById('sfCriticalHours')?.value||24));C.policy.employeeConfirmationUnderHours=Math.max(0,Number(document.getElementById('sfApprovalHours')?.value||24));C.policy.worksCouncilEnabled=!!document.getElementById('sfWorksCouncil')?.checked;C.persist();C.audit('COMPLIANCE_POLICY_UPDATED',null,{...C.policy});C.toast('Compliance-Einstellungen gespeichert','Betriebliche Schwellen und Freigaben wurden aktualisiert.')};
  if(typeof window.renderSettings==='function'){const base=window.renderSettings;window.renderSettings=function(){const r=base.apply(this,arguments);C.renderComplianceSettings();return r}}

  injectStyles();installEmployeeWorkModel();C.updateScheduleControls();C.decorateAssignments();if(document.getElementById('view-settings')?.classList.contains('active'))C.renderComplianceSettings();
  document.addEventListener('click',e=>{const v=e.target.closest('[data-view]')?.dataset.view;if(v==='schedule')setTimeout(()=>{C.updateScheduleControls();C.decorateAssignments()},0);if(v==='settings')setTimeout(C.renderComplianceSettings,0);if(v==='employees')setTimeout(installEmployeeWorkModel,0)});
})();
